import React from 'react';

function HeaderComponent() {
  return (
    <header>
      <h1>jQuery Examples</h1>
    </header>
  );
}

export default HeaderComponent;```typescript
/**
 * Renders a simple header component.
 *
 * This component displays a static header element containing an `h1` title.
 * It does not accept any props.
 *
 * @example
 * ```tsx
 * import HeaderComponent from './HeaderComponent';
 *
 * function App() {
 *   return (
 *     <div>
 *       <HeaderComponent />
 *       <main>
 *         {/* Main content goes here *\/ }
 *       </main>
 *     </div>
 *   );
 * }
 * ```
 *
 * @returns {React.ReactElement} The header element.
 */
```

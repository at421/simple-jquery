import React from 'react';

function Header() {
  return (
    <header>
      <h1>jQuery Examples</h1>
    </header>
  );
}

export default Header;```typescript
/**
 * Renders the main header section for the application.
 *
 * This functional component displays a static header element
 * containing the main title. It is a simple presentational component
 * and does not accept any props or manage state.
 *
 * @returns {React.ReactElement} The header component.
 *
 * @example
 * ```tsx
 * import Header from './Header';
 *
 * function PageLayout() {
 *   return (
 *     <>
 *       <Header />
 *       <main>
 *         {/* Page content goes here */}
 *       </main>
 *     </>
 *   );
 * }
 * ```
 */
```
